# تقرير الدفعة الثالثة (03): إدخال رمز التحقق OTP وإنشاء كلمة مرور جديدة

## الواجهات المنفذة
5. **شاشة إدخال رمز التحقق OTP (`OtpScreen`)**:
   - 6 خانات منفصلة تدعم التلقي والتنقل بين الخانات تلقائيًا.
   - دعم اللصق وإلغاء التركيز.
   - عداد تنازلي لميعاد إعادة الإرسال.
   - زر التأكيد التلقائي عند استكمال الخانات 6.
6. **شاشة تعيين كلمة المرور الجديدة (`ResetPasswordScreen`)**:
   - حقل إدخال كلمة المرور الجديدة مع تأكيدها.
   - التحقق من الحد الأدنى لطول كلمة المرور وتطابقها.
   - زر الحفظ وتأكيد التغيير.

## الملفات المضافة
- `lib/features/auth/otp_screen.dart`
- `lib/features/auth/reset_password_screen.dart`

## الفحص والاختبارات
- `flutter analyze`: نجاح بنسبة 100%.
- `flutter test`: نجاح.
