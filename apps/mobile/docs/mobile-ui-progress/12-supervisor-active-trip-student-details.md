# تقرير الدفعة الثانية عشرة (12): شاشة تنفيذ الرحلة وتفاصيل الطالب

## الواجهات المنفذة
23. **شاشة رحلة المشرفة (`SupervisorActiveTripScreen`)**:
    - أزرار كبيرة وعالية التباين تناسب الحركة والاستخدام داخل الباص.
    - تفاعل بضغطة واحدة: (صعد، لم يحضر، غائب).
    - زر التراجع عن آخر إجراء (Undo).
    - عدادات سريعة وحالة المزامنة والعمليات المعلقة.
24. **تفاصيل الطالب داخل الرحلة (`StudentInTripDetailsScreen`)**:
    - بيانات الطالب وملاحظات المحطة.
    - الاتصال السريع بولي الأمر بضغطة واحدة.

## الملفات المضافة
- `lib/features/supervisor/supervisor_active_trip_screen.dart`
- `lib/features/supervisor/student_in_trip_details_screen.dart`
